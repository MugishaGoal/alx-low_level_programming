#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @num1, @num2: numbers to be added
 * Return: 0
 *
 */

int add(int  num1, int num2)

{
	return (num1 + num2);
}
