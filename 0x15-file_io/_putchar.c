#include <stdio.h>

/**
 * _putchar - writes a character
 * @c: a character to be written
 *
 * Return: 1 on success, -1 on error and the error is set
 * appropriately
 */
int _putchar(char c)
{
	return (write (1, &c 1));
}
